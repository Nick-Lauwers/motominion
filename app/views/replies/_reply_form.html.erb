<div class="reply-form">
  <%= form_for @reply do |f| %>
  
    <%= render 'shared/error_messages', object: f.object %>
    
    <% if current_user.online? %>
      <div class="online-sm"></div>
    <% end %>
    
    <div class="reply-form-avatar">
      <%= image_tag (current_user.avatar.url) %>
    </div>
    
    <div class="reply-form-container">
      
      <%= f.text_field :content, 
                       placeholder: "Write a comment / answer.",
                       class: "form-control reply-form-control" %>
          
      <%= f.hidden_field :question_id, value: question_id %>
      
      <div class="reply-submit-container">
        <%= f.submit "Reply!", class: "reply-submit" %>
      </div>
    </div>
  <% end %>
</div>