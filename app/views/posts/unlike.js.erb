$('.unlike-post').bind('ajax:success', function(){
   $(this).parent().parent().find('.vote-count').html('<%= escape_javascript @post.votes_for.size.to_s %>');
   $(this).closest('.unlike-post').hide();
   $(this).closest('.vote-icon').html(' <%= link_to like_post_path(@post), remote: true, method: :get, class: 'like-post' do %> <i class="fa fa-thumbs-o-up fa-lg like-icon" aria-hidden="true"></i> <% end %>');
});