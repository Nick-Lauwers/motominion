<% provide(:title, 'Profile Photo') %>

<header class="header-static-gray">
  <%= render 'layouts/header_secondary' %>
</header>

<div class="credentials">
  <h1 class="credentials-title">Just a few details...</h1>
  <%= render 'profile_pic_form' %>
</div>